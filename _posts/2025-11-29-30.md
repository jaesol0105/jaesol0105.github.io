---
title:  "[29] UseCase로 가야하는 로직, ViewModel에 남아야하는 로직"
header:
  overlay_image: "https://image.artbox.co.kr/upload/C00001/goods/800_800/818/230525003912818.jpg?s=/goods/org/818/230525003912818.jpg"

excerpt: "usecase, viewmodel"

categories:
  - Flutter
tags:
  - [Flutter]
    
toc_label: "  목차"
toc: True
toc_sticky: True
---

# 2025-12-01

## UseCase로 가야 하는 것

### 비즈니스 검증 로직 ✨

```dart
if (todo.title.isEmpty) throw ValidationError();
```

### 데이터 변환 ✨

```dart
final normalizedTodo = todo.copyWith(
  title: todo.title.trim(),
  description: sanitize(todo.description),
);
```

### 복잡한 계산이나 대용량 데이터 처리


```dart
final priority = calculatePriority(todo, userPreferences);
```

```dart
final processedData = await processBulkData(eDataset);
```

<br><br>

## ViewModel에 남아야 하는 것

### UI 업데이트 조작에 관련된 로직 ✨

```dart
final idx = list.indexWhere((e) => e.id == todo.id);
state = AsyncData([...list]..[idx] = todo); // 낙관적 업데이트
```

### State 관리

```dart
state = AsyncLoading();
state = AsyncData(data);
state = AsyncError(error, stack);
```

<br><br>
